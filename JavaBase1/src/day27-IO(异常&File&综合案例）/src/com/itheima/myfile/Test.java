package com.itheima.myfile;

/*
 * 路径：
 * 		相对路径
 * 		绝对路径
 *
 * File：
 * 	- File对象就表示一个路径，可以是文件的路径，也可以是文件夹的路径
 * 	- 这个路径可以是存在的，也允许是不存在的
 *
 * 总结：
 * 		1.File对象表示路径，可以是文件、也可以是文件夹。
 * 	  		这个路径可以是存在的，也可以是不存在的
 * 		2.绝对路径和相对路径是什么意思？
 * 			绝对路径是带盘符的。
 * 			相对路径是不带盘符的，默认到当前项目下去找。
 * 		3.File三种构造方法的作用？
 * 			public File(String pathname)                根据文件路径创建文件对象（把字符串表示的路径变成File对象）
 * 			public File(String parent, String child)    根据父路径名字符串和子路径名字符串创建文件对象（把父级路径和子级路径进行拼接）
 * 			public File(File  parent, String child)     根据父路径对应文件对象和子路径名字符串创建文件对象（把父级路径和子级路径进行拼接）
 */
public class Test {
}
